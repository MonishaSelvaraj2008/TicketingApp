export interface Tokenresponse {
    id: any;
    firstName: string;
    lastName: string;
    mobile: string;
    email: string;
    password: string;
    accessToken: string;
    accessTokenExpiration: Date;
    refreshToken: string;
    refreshTokenExpiration: Date;
  }
